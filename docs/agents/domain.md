# Domain documentation

This is a single-context repository. Read `CONTEXT.md` before changing domain terms. Architecture decisions belong under `docs/adr/` only when they are hard to reverse, surprising, and based on a real tradeoff.
